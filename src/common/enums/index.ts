export enum AuthProvider {
  LOCAL = "local",
  GOOGLE = "google",
}

export enum OrganizationRole {
  OWNER = "owner",
  ADMIN = "admin",
  MEMBER = "member",
}

export enum SubscriptionStatus {
  ACTIVE = "active",
  TRIAL = "trial",
  INACTIVE = "inactive",
}
